type Task @model{
  id: ID!
  title: String!
  body: String!
  unique_File_ID: String
  state: TaskState!
  dueDate: Int
  cityName: String
}

enum TaskState {
  NEW
  ASSIGNED
  IN_PROGRESS
  COMPLETE
  HIGH_PRIORITY
}