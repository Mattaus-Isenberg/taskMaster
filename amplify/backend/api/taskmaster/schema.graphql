type Task @model @auth(rules: [{ allow: owner, operations: [update, delete] }]){
  id: ID!
  title: String!
  body: String!
  unique_File_ID: String
  state: TaskState!
  dueDate: Int
}

enum TaskState {
  NEW
  ASSIGNED
  IN_PROGRESS
  COMPLETE
  HIGH_PRIORITY
}